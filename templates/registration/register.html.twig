{% extends 'layouts/base.html.twig' %}

{% set title = "Register | " ~ website.name %}

{% block body %}
    <!-- New Career Section Heading-->
    <h2 class="page-section-heading text-center text-secondary text-uppercase font-weight-bold">{{ tab.register }}</h2>
    <!-- Icon Divider-->
    <div class="divider-custom">
        <div class="divider-custom-line"></div>
        <div class="divider-custom-icon"><i class="fas fa-star"></i></div>
        <div class="divider-custom-line"></div>
    </div>

    <div class="p-3 bg-white rounded shadow">
    {{ form_start(registrationForm) }}
        {{ form_widget(registrationForm) }}

        <input type="submit" class="btn btn-primary block" value="{{ button.validate }}" formnovalidate>
    {{ form_end(registrationForm) }}
    </div>
{% endblock %}
