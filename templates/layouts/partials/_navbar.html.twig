<nav class="navbar navbar-expand-md navbar-dark bg-dark bg-secondary font-weight-bold fixed-top">
    <div class="container">
        <b><a class="navbar-brand" href="{{ path('app_home') }}">{{ website.name|upper }}</a></b>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExample04" aria-controls="navbarsExample04" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarsExample04">
            <ul class="navbar-nav ml-auto py-2 px-0 px-lg-3 font-weight-bold text-uppercase">
            {% if app.user %}
                <li class="{{ set_active_route('app_home') }}">
                    <a class="nav-link" href="{{ path('app_home') }}">{{ navbar.home }}</a>
                </li>
                <li class="{{ set_active_route('app_career') }}">
                    <a class="nav-link" href="{{ path('app_career') }}">{{ navbar.career }}</a>
                </li>
                <li class="{{ set_active_route('app_project') }}">
                    <a class="nav-link" href="{{ path('app_project') }}">{{ navbar.projects }}</a>
                </li>
                <li class="{{ set_active_route('app_user_show') }}">
                    <a class="nav-link" href="{{ path('app_user_show') }}"><i class="fas fa-user"></i></a>
                </li>
                <li class="{{ set_active_route('') }}">
                    <a class="nav-link" href="{{ path('app_logout') }}"><i class="fas fa-sign-out-alt"></i></a>
                </li>
            {% else %}
                <li class="{{ set_active_route('app_register') }}">
                    <a class="nav-link" href="{{ path('app_register') }}"><i class="fas fa-user-plus"></i></a>
                </li>
                <li class="{{ set_active_route('app_login') }}">
                    <a class="nav-link" href="{{ path('app_login') }}"><i class="fas fa-sign-in-alt"></i></i></a>
                </li>
            {% endif %}
            </ul>
        </div>
    </div>
</nav>
