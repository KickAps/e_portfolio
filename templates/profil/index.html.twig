{% extends 'layouts/base.html.twig' %}

{% set title = "TODO | " ~ website.name %}

{% block body %}
    <!-- Profil Section Heading-->
    <h2 class="page-section-heading text-center text-secondary text-uppercase font-weight-bold">{{ profil.firstname ~ " ~ " ~ tab.profil }}</h2>
    <!-- Icon Divider-->
    <div class="divider-custom">
        <div class="divider-custom-line"></div>
        <div class="divider-custom-icon"><i class="fas fa-star"></i></div>
        <div class="divider-custom-line"></div>
    </div>
{% endblock %}
